'use strict';

const BOOLEAN = 'boolean';
const STRING = 'string';
const NUMBER = 'number';

const flagTypes = [NUMBER, STRING, BOOLEAN];

module.exports = {
	BOOLEAN,
	STRING,
	NUMBER,
	flagTypes,
};
